#include <stdio.h>

int main(){
    int nc = 10;
    int fattoriale = 1;
    while (nc >0){
        fattoriale=fattoriale*nc;
        nc--;v
    }
    printf ("\nFattoriale = %d" , fattoriale);
    char c = 'a';
    while (c != 'v'){
        printf("\npremi v");
        scanf("%c",&c);
        if (c != 'v'){
            printf("\nho detto v!!!");
        
        }

    } 
    return 0;
}
